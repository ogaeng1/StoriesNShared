import IconList from "./IconList";

const Navbar = () => {
  return (
    <div className="fixed top-0 w-full flex items-center justify-center bg-primary z-50">
      <IconList />
    </div>
  );
};

export default Navbar;
